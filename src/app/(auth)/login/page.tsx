import React from "react";

export const metadata = {
  title: "Login",
  description: "This is the login page",
};

const Login = () => {

  return (
    <div>
      <h2>Login Page</h2>
    </div>
  );
};

export default Login;
